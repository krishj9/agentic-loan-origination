"""Phase 4 tools package — deterministic tool backends.

Sub-packages:
  tools.llamaparse        — LlamaParse wrapper + canonical normalization (P4-T1/T2)
  tools.risk_engine       — Deterministic mock risk engine (P4-T3/T4)
  tools.compliance_engine — Rule-based compliance engine (P4-T5)
  tools.packager          — PDF/JSON artifact packager + S3 archival (P4-T6/T7)
"""
